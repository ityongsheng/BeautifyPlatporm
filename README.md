# Beauty Platform AI - 智享美妆推荐系统

这是一个基于 Django + Vue + DeepSeek 大模型的个性化美妆推荐与咨询系统。系统包含个性化推荐引擎、AI 美妆顾问、全平台运营监控看板以及自动化 API 文档管理。

## 🚀 Windows 系统启动指南

### 1. 环境准备
确保你的系统已安装 **Anaconda** 或 **Miniconda**。

### 2. 创建并激活虚拟环境
打开命令提示符 (CMD) 或 PowerShell：
```bash
# 创建环境（如果尚未创建）
conda create -n djangoBeauty python=3.11

# 激活环境
conda activate djangoBeauty
```

### 3. 安装依赖
在项目根目录下运行：
```bash
pip install -r requirements.txt
```

### 4. 初始化数据库
执行 Django 迁移命令以同步数据库结构：
```bash
python manage.py makemigrations
python manage.py migrate
```

### 5. 启动后端服务 (Django)
```bash
python manage.py runserver 0.0.0.0:8000
```
后端 API 将运行在 [http://127.0.0.1:8000](http://127.0.0.1:8000)。

### 6. 启动前端服务
为了正确处理跨域请求并模拟生产环境，建议使用静态服务器启动前端。在 `frontend` 目录下运行：
```bash
cd frontend
python -m http.server 8080
```
前端界面将运行在 [http://127.0.0.1:8080/index.html](http://127.0.0.1:8080/index.html)。

---

## 🛠 管理端功能
- **监控大屏**: 在首页点击“管理员入口”或直接访问 `admin.html` 查看 ECharts 实时数据。
- **文档自动化**: 运行 `python app/api_docs.py` 可自动更新 `api_spec.json` 接口文档。

---

## 📤 Git 推送代码命令

如果你对代码进行了修改，请按以下步骤将其推送到 GitHub 仓库：

### 1. 查看变更状态
```bash
git status
```

### 2. 添加变更到暂存区
```bash
# 添加所有文件
git add .
```

### 3. 提交变更
```bash
git commit -m "你的提交说明（例如：feat: 增加看板功能）"
```

### 4. 推送到远程仓库
```bash
git push origin main
```

---

## 🏗 技术栈清单
- **后端**: Django, Django REST Framework, OpenAI (DeepSeek API)
- **数据分析**: Pandas, Scikit-learn (用于协同过滤召回)
- **前端**: Vanilla JS, Vanilla CSS, ECharts 5.4
- **文档**: Swagger/OpenAPI (自动化生成), Mermaid Flowcharts
